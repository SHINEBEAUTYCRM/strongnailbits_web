import { PlaceholderPage } from "@/components/admin/PlaceholderPage";
import { CheckSquare } from "lucide-react";

export default function TasksPage() {
  return <PlaceholderPage title="Задачі" icon={CheckSquare} />;
}
