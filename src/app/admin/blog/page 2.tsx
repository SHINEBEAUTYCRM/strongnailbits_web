import { PlaceholderPage } from "@/components/admin/PlaceholderPage";
import { PenSquare } from "lucide-react";

export default function BlogPage() {
  return <PlaceholderPage title="Блог" icon={PenSquare} />;
}
