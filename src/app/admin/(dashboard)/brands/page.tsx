import { PlaceholderPage } from "@/components/admin/PlaceholderPage";
import { Award } from "lucide-react";

export default function BrandsPage() {
  return <PlaceholderPage title="Бренди" icon={Award} />;
}
