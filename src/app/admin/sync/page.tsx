import { PlaceholderPage } from "@/components/admin/PlaceholderPage";
import { RefreshCw } from "lucide-react";

export default function SyncPage() {
  return <PlaceholderPage title="Синхронізація" icon={RefreshCw} />;
}
